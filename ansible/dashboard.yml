---
- name: Create PK and FK for tables and index
  hosts: db_servers
  roles:
    - keys_and_index
  vars:
    schema: small
  tags: index
  
- name: Create MATERIALIZED VIEW
  hosts: db_servers
  roles:
    - materialized
  vars:
    schema: small
  tags: materialized-view

- name: Mod database
  hosts: db_servers
  roles:
    - tuning
  vars:
    schema: small
  tags: mod-database